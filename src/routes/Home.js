import React from 'react';

import Header from '../components/Header'
import ListForm from './../containers/ListForm'

const Home = () => {

    return (
        <div>
            <Header isShowLink />
            <ListForm />
        </div>

    );
}

export default Home;
